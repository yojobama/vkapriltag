diff --git a/common/pthreads_cross.c b/common/pthreads_cross.c
index 04e556c..f3315cc 100644
--- a/common/pthreads_cross.c
+++ b/common/pthreads_cross.c
@@ -22,7 +22,12 @@ SOFTWARE.
 
 #include "common/pthreads_cross.h"
 
-#ifdef _WIN32
+// See pthreads_cross.h: MinGW links against a real winpthreads pthread_*
+// implementation, so none of the CRITICAL_SECTION/CONDITION_VARIABLE-based
+// emulation below is compiled (or needed) there - only pcthread_get_num_procs
+// further down is common to every _WIN32 target, MinGW included, since
+// sysconf(_SC_NPROCESSORS_ONLN) isn't available on MinGW's headers either.
+#if defined(_WIN32) && !defined(__MINGW32__)
 
 typedef struct {
     SRWLOCK lock;
@@ -238,6 +243,10 @@ unsigned int timespec_to_ms(const struct timespec *abstime)
     return ((abstime->tv_sec - time(NULL)) * 1000) + (abstime->tv_nsec / 1000000);
 }
 
+#endif /* defined(_WIN32) && !defined(__MINGW32__) */
+
+#ifdef _WIN32
+
 unsigned int pcthread_get_num_procs()
 {
     SYSTEM_INFO sysinfo;
diff --git a/common/pthreads_cross.h b/common/pthreads_cross.h
index fc40c9b..5aabf12 100644
--- a/common/pthreads_cross.h
+++ b/common/pthreads_cross.h
@@ -23,16 +23,24 @@ SOFTWARE.
 #ifndef __CPTHREAD_H__
 #define __CPTHREAD_H__
 
-#ifdef _WIN32
+// MinGW-w64 ships a real winpthreads <pthread.h> (unlike MSVC, which has
+// none), so on MinGW this header must stay out of the way and let that
+// real implementation provide the pthread_* types/functions - defining our
+// own emulated typedefs of the same names alongside it is a hard
+// redefinition conflict, not just a style choice.
+#if defined(_WIN32) && !defined(__MINGW32__)
 #include <stdbool.h>
 #include <windows.h>
 #else
 #include <pthread.h>
 #include <sched.h>
 #endif
+#ifdef _WIN32
+#include <windows.h>
+#endif
 #include <time.h>
 
-#ifdef _WIN32
+#if defined(_WIN32) && !defined(__MINGW32__)
 
 typedef CRITICAL_SECTION pthread_mutex_t;
 typedef void pthread_mutexattr_t;
